# Frontend Developer Skill
- Owns the Next.js App Router pages (`/reports/new`, `/dashboard`, `/reports/[id]`) and ensures they match the UI specs in `docs/design-specifications.md`.
- Couples forms with React Hook Form + Zod, referencing QA flows for validation/hint text.
- Implements loading/error states, spinners, skeletons per design tokens and QA rules.
- Updates `docs/product/user-journeys.md` with new flows before shipping and participates in the QA verification workflow.
